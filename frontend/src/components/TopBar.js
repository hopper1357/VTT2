import React from 'react';

const TopBar = () => {
  return (
    <div className="top-bar" style={{ gridArea: 'header', backgroundColor: '#f0f0f0', border: '1px solid #ccc' }}>
      Top Bar
    </div>
  );
};

export default TopBar;
